//in progress and may not work
create.import(customCheck)
import @media
import game
import window
import update
(input)text {
img = desktop.import (type= 'button' onclick)resolute[func.file.export]
}
let media= media + 50
log media
